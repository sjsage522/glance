# GitHub Token for releases widget
# For local development, you can use a Personal Access Token:
# Create one at https://github.com/settings/tokens with 'public_repo' scope
# In GitHub Actions, the built-in GITHUB_TOKEN will be used automatically
GITHUB_TOKEN=your_github_token

# Any other environment variables needed by your widgets